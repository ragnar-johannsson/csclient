#!/usr/bin/env watchr

def run_all_tests
  print `clear`
  puts "Tests run #{Time.now.strftime('%Y-%m-%d %H:%M:%S')}"
  puts `./run_tests`
end

run_all_tests
watch("(test|lib)(/.*)*(/.*)+.js") { |m| run_all_tests }
